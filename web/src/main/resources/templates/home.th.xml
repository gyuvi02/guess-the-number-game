<thlogic>
    <attr sel="#headId" th:replace="~{fragments/head :: headFragment}"/>
    <attr sel="#welcome-text" th:text="#{game.title}"/>

    <attr sel="#playLink"
          th:href="@{__${T(org.gyula.util.GameMappings).PLAY}__}"
          th:text="#{play.link.text}"/>
    <attr sel="#footerId" th:replace="~{fragments/footer :: footerFragment}"/>
</thlogic>